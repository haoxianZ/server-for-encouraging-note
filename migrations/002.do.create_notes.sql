CREATE TABLE notes (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    content TEXT NOT NULL,
    Liked INTEGER NOT NULL
    user_id INTEGER REFERENCES users(serialId) ON DELETE CASCADE NOT NULL
);