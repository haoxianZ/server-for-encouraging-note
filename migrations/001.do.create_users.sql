CREATE EXTENSION IF NOT EXISTS pgcrypto ;

CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    username TEXT NOT NULL UNIQUE,
    email TEXT,
    serialId INTEGER GENERATED BY DEFAULT AS IDENTITY UNIQUE
);
